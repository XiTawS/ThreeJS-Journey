{
  "name": "threejs-journey",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Three.js Journey - Collection de projets d'apprentissage",
  "scripts": {
    "build": "node build.js",
    "html": "node build.js --html-only",
    "serve": "npx serve dist -l 3000",
    "preview": "npm run html && npm run serve",
    "build:all": "npm run build",
    "clean": "node clean.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "gsap": "3.12"
  }
}